import {session} from '@/lib/auth';
import {logout} from '@/app/actions';
import {Navigation} from '@/components/navigation';
export const dynamic='force-dynamic';
export default async function Workspace({children}:{children:React.ReactNode}){const {member,manager}=await session();return <div className="app-shell"><aside className="sidebar"><a href="/" className="wordmark">ZERO<span>CLIPS</span><i/></a><div className="workspace-label">ESPACIO DE TRABAJO</div><Navigation manager={manager}/><div className="sidebar-bottom"><div className="live-dot">Operación administrativa</div><small>MDM pendiente de configurar</small><div className="user"><div className="avatar">{member.name.slice(0,2).toUpperCase()}</div><div><strong>{member.name}</strong><small>{member.role}</small></div></div><form action={logout}><button className="logout">Cerrar sesión ↗</button></form></div></aside><main className="main"><header className="topbar"><span>Espacio <span className="muted">/</span> <strong>ZEROCLIPS</strong></span><form action={logout} className="mobile-logout"><button className="logout">Cerrar sesión ↗</button></form><span className="top-tag"><span className="dot"/> Operación</span></header><div className="content">{children}</div><footer>ZEROCLIPS <span>Control de flota y producción</span></footer></main></div>;}
