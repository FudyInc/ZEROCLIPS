import {previewEnabled} from '@/lib/preview';import {redirect} from 'next/navigation';
import {session} from '@/lib/auth';import {setPassword} from './password-action';
export default async function Password({searchParams}:{searchParams:Promise<{error?:string}>}){if(previewEnabled())redirect('/');await session();const {error}=await searchParams;return <main className="setup"><div className="wordmark">ZERO<span>CLIPS</span></div><h1>Define tu contraseña</h1>{error&&<p className="error">{error}</p>}<form action={setPassword}><label>Nueva contraseña (mínimo 12 caracteres)<input name="password" type="password" autoComplete="new-password" minLength={12} required/></label><button className="primary">Guardar y acceder</button></form></main>;}
