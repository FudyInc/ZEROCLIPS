import {login} from '@/app/actions';
import {configured} from '@/lib/supabase';
import {redirect} from 'next/navigation';
export const dynamic='force-dynamic';
export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}){if(!configured())redirect('/setup');const {error}=await searchParams;return <main className="auth"><div className="auth-art"><div className="wordmark">ZERO<span>CLIPS</span></div><div><span className="eyebrow">TU OPERACIÓN, BAJO CONTROL</span><h1>Menos fricción.<br/>Más contenido.</h1><p>Organiza tus equipos, conecta a tu equipo y sigue cada producción.</p></div><small>Plataforma de operaciones · v0.1</small></div><section className="auth-form"><span className="eyebrow">BIENVENIDO A ZEROCLIPS</span><h2>Tu próximo gran clip<br/>empieza aquí.</h2><p className="muted">Accede con tu cuenta de trabajo.</p>{error&&<p role="alert" className="error">{error}</p>}<form action={login}><label>Correo electrónico<input name="email" type="email" autoComplete="username" required placeholder="nombre@empresa.com"/></label><label>Contraseña<input name="password" type="password" autoComplete="current-password" required minLength={8}/></label><button className="primary">Iniciar sesión →</button></form><p className="muted small">Acceso por invitación. Solicita tu cuenta al administrador.</p></section></main>;}
